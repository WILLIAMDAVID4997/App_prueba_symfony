{% extends 'base.html.twig' %}

{% block title %}Alumnos{% endblock %}

{% block body %}
    <h1>Alumnos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alumno.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ alumno.nombre }}</td>
            </tr>
            <tr>
                <th>Apellidos</th>
                <td>{{ alumno.apellidos }}</td>
            </tr>
            <tr>
                <th>Correo_electronico</th>
                <td>{{ alumno.correoElectronico }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_alumnos_index') }}">Regresar</a>

    <a href="{{ path('app_alumnos_edit', {'id': alumno.id}) }}">Editar</a>

    {{ include('alumnos/_delete_form.html.twig') }}
{% endblock %}
