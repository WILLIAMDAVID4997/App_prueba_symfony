<form method="post" action="{{ path('app_cursos_delete', {'id': curso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ curso.id) }}">
    <button class="btn">Eliminar</button>
</form>
